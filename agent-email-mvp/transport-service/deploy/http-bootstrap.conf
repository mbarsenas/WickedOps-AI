server {
    listen 80 default_server;
    server_name _;
    return 404;
}
server {
    listen 80;
    server_name mail.senderpermit.com;
    location ^~ /.well-known/acme-challenge/ {
        root /var/www/senderpermit-acme;
        default_type text/plain;
    }
    location / { return 404; }
}
